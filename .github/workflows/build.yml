name: Build EXE

on: [push]

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Build EXE
        run: pyinstaller --onefile --hidden-import=pypdf your_script.py

      - name: Upload EXE
        uses: actions/upload-artifact@v4
        with:
          name: exe-output
          path: dist/
